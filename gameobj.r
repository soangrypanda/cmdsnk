#ifndef CMDSNK_GAMEOBJ_H_SENTRY
#define CMDSNK_GAMEOBJ_H_SENTRY

/* EVERY GAME OBJECT (BOTH 2 AND 3D?) SHOULD HAVE
    * collection of its coordinates
    * pointer to its description (is given by a user?)
    * collection of pointers to its spites
    * velocity
    * direction of movement 
/*

#endif
